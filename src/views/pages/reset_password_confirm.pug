extends ../layout/default

block content
    .fullpage
        form.form
            p.form__title Введите новый пароль
            label.field
                input(placeholder="Пароль" name="login" type="input")
            label.field
                //- +icon('SecurityRounded')
                input(placeholder="Пароль" name="password" type="password")
                span.field__password-hide
                    +icon('RemoveRedEyeRounded')
            button(type="submit").btn.btn__join Подтвердить
            button(type="button").btn.btn__create Уже есть аккаунт